# Copy to .env.local. Never commit real values.
NEXT_PUBLIC_SITE_URL=https://okwan.ai

# Supabase (server only). The waitlist returns 503 until these are set.
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=

# Gemini API: pre-rendered officer audio (pnpm audio:hero) and, next, the live officer.
GEMINI_API_KEY=
# GEMINI_TTS_MODEL=gemini-3.8-flash-tts
